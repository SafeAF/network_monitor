# Guardrails
- Do not change Gemfile/Gemfile.lock unless explicitly requested.
- Do not change README unless explicitly requested.
- Do not change docs except the one being executed.
- Before any commit: show `git diff --stat` and justify each file in one sentence.
